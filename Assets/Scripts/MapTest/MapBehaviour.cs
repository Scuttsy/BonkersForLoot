using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MapBehaviour : MonoBehaviour
{

    ////TODO
    ///SpinningPlatforms 
    /// <summary>
    /// The centre bridges surrounding the dunk zone must spin
    /// The circular platforms must also spin
    /// </summary>
    /// Columns must go up and down without affecting the Y position of the player on collisions


    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
